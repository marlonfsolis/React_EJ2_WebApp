{
    "version": "0.2.0",
    
    "configurations": [
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "preLaunchTask": "npm run start",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceRoot}/src"
        },
        
    ]
}